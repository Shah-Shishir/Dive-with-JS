// Joining_Arrays

let numbers = [1,2,3];

console.log(numbers.join());
console.log(numbers.join('-'));
console.log(numbers.join(':'));
console.log(numbers.join(','));

let words = 'a-b-c-d';
console.log(words.split('-'));
